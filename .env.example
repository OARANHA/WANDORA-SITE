# Local development database. Prisma resolves relative SQLite paths from prisma/schema.prisma.
DATABASE_URL="file:../db/wandora-site.db"

# Docker entrypoint: synchronize the Prisma schema before the app starts.
# Destructive schema changes are not accepted automatically by Prisma db push.
WANDORA_SITE_DB_PUSH="1"
